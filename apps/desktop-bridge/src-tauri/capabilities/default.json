{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-read-only",
  "description": "Minimum capability set for the read-only CyberTools Desktop Bridge window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
