name: Desktop Bridge Validation

"on":
  workflow_dispatch: {}
  pull_request:
    paths:
      - 'apps/desktop-bridge/**'
      - '.github/workflows/desktop-bridge.yml'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm GitHub Actions runner starts
        run: echo "Desktop Bridge workflow runner started"
